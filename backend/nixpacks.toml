# Railway/Nixpacks configuration
# Note: garmindb installation removed for initial deployment
# The app will work for registration, login, and Garmin connection
# Sync feature can be added later once basic deployment works
[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "npm install"
]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "node server.js"

